<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MOVIE VERSE Admin Panel</title>
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.7/css/all.css">
  <style>
    body {
      font-family: "Rubik", sans-serif;
      background-color: #121212;
      margin: 0;
      padding: 0;
      color: #edf2f4;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #1f1f1f;
      padding: 10px 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
    }

    .header h1 {
      font-size: 24px;
      color: #d90429;
    }

    .header a {
      text-decoration: none;
      color: #edf2f4;
      font-weight: bold;
      transition: color 0.3s;
    }

    .header a:hover {
      color: #d90429;
    }

    .container {
      max-width: 1200px;
      margin: 20px auto;
      padding: 20px;
      background-color: #1f1f1f;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    }

    h2 {
      color: #d90429;
      text-align: center;
      margin-bottom: 20px;
    }

    .form {
      max-width: 600px;
      margin: auto;
    }

    input,
    select,
    button {
      width: 100%;
      padding: 15px;
      margin-bottom: 15px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      font-family: "Rubik", sans-serif;
    }

    input {
      background-color: #2b2b2b;
      color: #edf2f4;
    }

    input::placeholder {
      color: #a9a9a9;
    }

    select {
      background-color: #2b2b2b;
      color: #edf2f4;
    }

    input:focus,
    select:focus {
      outline: none;
      border: 2px solid #d90429;
    }

    button {
      background-color: #d90429;
      color: #edf2f4;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #a6001d;
    }

    .form-btns {
      display: flex;
      justify-content: space-between;
    }

    .form-btns a {
      text-align: center;
      display: inline-block;
      padding: 15px 20px;
      background-color: #2b2b2b;
      color: #edf2f4;
      border-radius: 5px;
      text-decoration: none;
      font-size: 16px;
      font-weight: bold;
      transition: background-color 0.3s, color 0.3s;
    }

    .form-btns a:hover {
      background-color: #d90429;
      color: #fff;
    }

    .footer {
      text-align: center;
      margin-top: 30px;
      padding: 10px;
      background-color: #1f1f1f;
      color: #edf2f4;
    }

    .footer a {
      text-decoration: none;
      color: #d90429;
      font-weight: bold;
    }
  </style>
</head>

<body>

  <div class="header">
    <h1>MOVIE VERSE Admin Panel</h1>
    <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
  </div>

  <div class="container">
    <h2>Upload a New Movie</h2>
    <div class="form">
      <form action="/insert" method="post" enctype="multipart/form-data">
        <input type="text" placeholder="Movie Name" name="name" required />
        <input type="text" placeholder="Movie Description" name="desc" required />
        <input type="number" placeholder="Movie Price" name="price" min="1" step="any" required />
        <input type="file" placeholder="Upload Movie Image" name="image" accept="image/*" required />
        <div class="form-btns">
          <button type="submit">Submit</button>
          <a href="/views"><i class="fas fa-eye"></i> View Movies</a>
        </div>
      </form>
    </div>
  </div>

  <div class="footer">
    <p>&copy; 2024 MOVIE VERSE. Designed by Admin.</p>
  </div>

</body>

</html>
